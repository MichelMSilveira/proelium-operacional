"use client";
import { FormEvent, useEffect, useState } from "react";
import { useParams } from "next/navigation";
import { ModuleLayout } from "../../components/ModuleLayout";
import { apiGet, apiPut } from "../../../lib/api";
type Item = Record<string, unknown>; type Payload = { revision?: number; data?: Record<string, Item[]> };
export default function QuoteDetailPage() { const { id } = useParams<{ id: string }>(); const [payload, setPayload] = useState<Payload | null>(null); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); useEffect(() => { apiGet<Payload>("/api/data").then(setPayload).catch((reason: unknown) => setError(reason instanceof Error ? reason.message : "Falha ao carregar o orçamento.")); }, []); const quote = payload?.data?.quotes?.find((item) => String(item.id) === id); const rooms = (payload?.data?.quoteRooms || []).filter((item) => String(item.quoteId) === id); async function createRoom(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!payload?.data) return; const name = String(new FormData(event.currentTarget).get("name") || "").trim(); if (!name) return; const quoteRooms = [...(payload.data.quoteRooms || []), { id: `amb-next-${crypto.randomUUID()}`, quoteId: id, name, items: [] }]; setSaving(true); setError(""); try { const result = await apiPut<{ revision?: number }>("/api/data", { data: { ...payload.data, quoteRooms }, baseRevision: payload.revision || 0 }); setPayload({ revision: result.revision, data: { ...payload.data, quoteRooms } }); event.currentTarget.reset(); } catch (reason) { setError(reason instanceof Error ? reason.message : "Não foi possível criar o ambiente."); } finally { setSaving(false); } } return <ModuleLayout eyebrow="COMERCIAL" title={String(quote?.title || "Orçamento")} description="Detalhe do orçamento e ambientes vinculados.">{error && <p className="error">{error}</p>}{!error && !quote && <p>Orçamento não encontrado.</p>}{quote && <><p>Status: <strong>{String(quote.status || "Rascunho")}</strong> · Versão {String(quote.version || 1)}</p><form className="create-form" onSubmit={createRoom}><input name="name" placeholder="Nome do ambiente" required /><button disabled={saving}>{saving ? "Salvando…" : "Adicionar ambiente"}</button></form><section className="record-list">{rooms.map((room, index) => <article key={String(room.id || index)}><strong>{String(room.name || `Ambiente ${index + 1}`)}</strong><span>{Array.isArray(room.items) ? `${room.items.length} item(ns)` : "Nenhum item"}</span></article>)}{rooms.length === 0 && <p>Nenhum ambiente cadastrado.</p>}</section></>}</ModuleLayout>; }
